# ─── N Stock LINE Bot — Environment Variables ────────────────────────────────
# Copy ไฟล์นี้เป็น .env แล้วใส่ค่าจริง (อย่า commit .env ขึ้น GitHub)

LINE_CHANNEL_SECRET=ใส่_Channel_Secret_ที่นี่
LINE_CHANNEL_ACCESS_TOKEN=ใส่_Channel_Access_Token_ที่นี่

PORT=3000
